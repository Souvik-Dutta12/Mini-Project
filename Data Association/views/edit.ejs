<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Association</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">

            <a class="rounded-md bg-red-500 px-3 py-2 text-sm mb-5 inline-block" href="/logout">Log Out</a>
        </div>
        <h5 class="mb-5">Edit your Profile</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind ?" class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 rounded-md border-zinc-700" name="content" ><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40" type="submit" value="Update Post">
        </form>
        
    </div>
</body>
</html>